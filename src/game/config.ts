import type { GameConfig } from "./types";

export const CONFIG: GameConfig = {
  logicalWidth: 500,
  logicalHeight: 800,
  ballRadius: 15,
  pinRadius: 10,
  initialBalls: 9,
  winReward: 5,
  numGates: 6,
  gateWidth: 70,
  scale: 10,
  tunnelWidth: 45,
};
